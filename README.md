# my-react-app

Learning React by building a simple app (TypeScript)

## Basic React commands

install packages:

```bash
npm i
```

update packages:

```bash
npm update
```

run dev server:

```bash
npm run dev
```
